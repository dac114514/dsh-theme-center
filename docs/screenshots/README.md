# Screenshots

Real captures used by the READMEs. Keep images reasonably sized for GitHub
(< 1 MB each is a good rule of thumb); PNG is fine for UI captures.

| File | Contents |
| ---- | -------- |
| `theme.png` | The theme gallery (效果图) — light/dark grouped, 3-column grid |
| `theme-window.png` | The theme menu inside the settings panel (设置面板的主题菜单) |
| `theme-edit.png` | The custom wallpaper crop/tint editor (自定义壁纸编辑界面) |

Both READMEs reference these by path, e.g.
`docs/screenshots/theme.png`. If you replace a capture, keep the same
basename and update the image in place so the links stay valid.
